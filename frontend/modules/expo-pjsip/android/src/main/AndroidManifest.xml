<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <!-- Permissions + the SipForegroundService <service> entry are injected into the
       APP manifest by app.plugin.js during `expo prebuild`. -->
</manifest>
